{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile.generator"
  },
  "deploy": {
    "startCommand": "python -m src.produce",
    "cronSchedule": "0 2 * * *",
    "restartPolicyType": "NEVER"
  }
}
