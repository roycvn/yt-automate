# yt-automate

Automated multi-language YouTube publishing pipeline. Takes owned source
videos, applies a channel watermark, generates localized (Hindi + Telugu)
versions, clips them into Shorts, generates SEO, and publishes to YouTube.

Integrates with the klipr batch API for clipping, dubbing, watermarking, and
caption burning. See `PLAN.md` for the full architecture and build plan.
